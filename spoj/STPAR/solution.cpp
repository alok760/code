#include<iostream>
using namespace std;
int main()
{
  int n;
  cin>>n;
  while(n!=0)
  {
    int count = 1, ocount = INT_MAX;
    for(int i=0;i<n;i++)
      {
        cin>>temp;
        if(temp == count)
          count++;
        else
          if(temp > ocount)
      }

  }
  return 0;
}
